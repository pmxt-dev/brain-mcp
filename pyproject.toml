[project]
name = "brain-mcp"
version = "0.1.0"
description = "MCP server for the pmxt-dev Company Brain"
dependencies = [
    "mcp[stdio]>=1.2.0",
    "httpx>=0.24.0",
]

[project.scripts]
brain-mcp = "brain_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
